import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ArraysPage } from './arrays';

@NgModule({
  declarations: [
    ArraysPage,
  ],
  imports: [
    IonicPageModule.forChild(ArraysPage),
  ],
})
export class ArraysPageModule {}
